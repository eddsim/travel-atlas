name: Update Phuket itinerary

on:
  push:
    paths:
      - '.github/workflows/update-phuket-safe-itinerary.yml'
      - 'scripts/update-phuket-safe-itinerary.mjs'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Update itinerary HTML
        run: node scripts/update-phuket-safe-itinerary.mjs
      - name: Commit updated guide
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/trips/2026/09-24-phuket/index.html
          if git diff --cached --quiet; then
            echo "No itinerary changes to commit"
          else
            git commit -m "Update Phuket surf and dining itinerary"
            git push
          fi
